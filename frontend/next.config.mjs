/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  allowedDevOrigins: [
    "scheduled-thesis-responsibilities-wondering.trycloudflare.com",
    "192.168.1.4",
  ],
};

export default nextConfig;